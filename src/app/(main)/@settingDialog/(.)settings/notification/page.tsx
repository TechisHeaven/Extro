import React from "react";
import SettingDialog from "../../SettingDialog";
import Notification from "@/app/(main)/settings/notification/page";

const DialogMe = () => {
  return (
    <SettingDialog>
      <Notification />
    </SettingDialog>
  );
};

export default DialogMe;
